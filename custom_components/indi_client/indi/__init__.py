"""Pure-Python INDI protocol client, independent of Home Assistant.

This package only depends on the standard library so it can be unit
tested (and reused) without a running Home Assistant instance.
"""
